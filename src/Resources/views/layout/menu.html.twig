{% import '@MobizelMarkdownDocs/macro/menu.html.twig' as menu %}

<ul class="nav flex-column">
    {% for item in menu_items %}
        {% if item.slug != 'index' %}
            {{ menu.nav_item(item, current_item, current_route) }}
        {% endif %}
    {% endfor %}
</ul>
