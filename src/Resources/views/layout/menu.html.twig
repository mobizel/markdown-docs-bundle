{% import '@MobizelMarkdownDocs/macro/menu.html.twig' as menu %}

<ul class="nav flex-column">
    {% for item in menu_items %}
        {{ menu.nav_item(item.slug, item.path, current_item, current_sub_menu_items) }}
    {% endfor %}
</ul>
